# Demo shot list (≈30s)

Contrast first, then success. Do not only show the working timeline.

1. CapCut home → open an **externally created** draft (or a leftover CLI draft) → flash the **“unconventional path”** rejection dialog (~3–5s).
2. Back to home → open App-created shell `shell-opp2` (must open cleanly).
3. Show timeline tracks: video ~5s, two English text cues, voiceover ~3s.
4. Play or scrub so both subtitle lines appear in the preview.

Export `docs/demo.mp4` (1080p; avoid face/account UI if possible) before making the repo public.
