# Demo shot list (≈30s)

1. CapCut home → click project `shell-opp2` (must open cleanly).
2. Show timeline tracks: video ~5s, two English text cues, voiceover ~3s.
3. Play or scrub so both subtitle lines appear in the preview.

Export `docs/demo.mp4` (1080p, no face/account UI if possible) before making the repo public.
